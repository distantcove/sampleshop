<!DOCTYPE html>
<html>
  <head>
    <title>Sample Shoppy</title>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <link href="https://fonts.googleapis.com/css?family=Lato|Open+Sans|Oswald|PT+Sans|Permanent+Marker|Raleway|Roboto" rel="stylesheet">


    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
  </head>

  <body>
  	<nav class= "navbar navbar-default navbar-fixed-top">
      <div class="container" id="nav-container">
      	<ul class = "nav nav-pills">
          <li role="presentation" class="active"><%= link_to "Home", root_path %></li>
  	      <li role="presentation" class="active"><%= link_to "Laundry", static_pages_schedule_path%></li>
  	      <li role="presentation" class="active"><%= link_to "Messages", messages_path %></li>
  	    <!-- </ul>
        <ul class="nav pull-right"> -->
          <li id="search-form">
          <%= form_tag("/messages", method: "get") do %>
            <%= label_tag(:q, "Search Messages:") %>
            <%= text_field_tag(:q) %>
            <%= submit_tag("Search") %>
          <% end %>   	  
          </li>
          <li id="login-form">
          <% if user_signed_in? %>
            <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
          <% else %>
            <%= link_to('Login', new_user_session_path) %>
          <% end %>
          </li>  
        </ul>      
    </nav>
    
    <div>
      <% if notice %>
        <p id="notice" class="alert alert-success"><%= notice %></p>
      <% end %>
    </div>

    <div>
      <% if alert %>
        <p id="alert" class="alert alert-warning"><%= alert %></p>
      <% end %>
    </div>

    <%= yield %>

    <footer class="footer">
      <div class="container" id="footer-container">
      	&copy: 2016 DistantCoveDesigns
      </div>
    </footer>

  </body>
</html>
